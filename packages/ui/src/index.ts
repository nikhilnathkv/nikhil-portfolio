/**
 * Shared UI component library. Empty for M1 — components are extracted here as
 * the public web app and admin CMS start to share primitives (buttons, cards,
 * metric tiles, etc.).
 */
export {};
